---
import BaseLayout from "../../layouts/BaseLayout.astro";

export function getStaticPaths() {
  return [
    { params: { id: "test" } },
  ];
}

const { id } = Astro.params;
---

<BaseLayout title={`Node: ${id}`}>
  <h1>Node</h1>
  <p>{id}</p>
</BaseLayout>